version: '3.8'
services:
  whatsapp-linker:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - SERVER_URL=https://whapp.xia.ar/
    restart: always
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.whatsapp.rule=Host(`tu-subdominio.midominio.com`)"
      - "traefik.http.services.whatsapp.loadbalancer.server.port=3000"